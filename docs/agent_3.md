# Agent 3 Role

Primary function:

- simplify the solution and remove avoidable performance or maintenance debt

Execution rules:

- reduce complexity where the implementation drifted
- verify the change remains readable and direct
- check performance-sensitive paths and avoid unnecessary abstraction
- leave the code easier to reason about than it was before
