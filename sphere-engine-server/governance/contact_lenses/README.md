# Contact Lenses

Place one JSON file per agent DID in this directory.
All files must validate against `../contact_lens_schema.json`.
At least one contact lens file is required for governance policy load to succeed.
