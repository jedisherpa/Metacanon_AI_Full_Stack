{
  "name": "council-engine",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "engine",
    "skins/council-nebula"
  ],
  "scripts": {
    "check:command-parity": "node scripts/check-tma-command-catalog-parity.mjs",
    "check:bridge-parity": "node scripts/check-runtime-bridge-parity.mjs",
    "hooks:install": "git config core.hooksPath .githooks",
    "test:integration:pg": "npm run test:integration:pg -w engine",
    "test:redteam:pg": "npm run test:redteam:pg -w engine",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1"
  }
}
